# Status Progress — PlayArena (Booking Venue Olahraga)

> File ini dipakai sebagai checkpoint lintas-sesi. Lanjutkan di sesi chat baru dengan minta Claude baca file ini dulu.

## Ringkasan Proyek

Platform booking online untuk venue olahraga (futsal, badminton, basket, tenis, dsb) — **satu pemilik usaha, bisa multi-venue** (seperti cabang, bukan marketplace terbuka). Nama produk: **PlayArena** (sebelumnya nama kerja-sementara "GORin", diganti 2026-08-27 sesuai logo resmi yang sudah dibuat user — lihat `logo-rec-center.jpg`).

## 📄 PRD

Dibuat sebagai artifact HTML interaktif (sidebar TOC, 21 modul fitur dalam 3 fase, tabel peran & akses, tech stack, alur booking, entitas data inti, roadmap, batasan & non-goal), sudah di-rebrand pakai logo &amp; palet warna PlayArena (biru/oranye) yang asli:
**https://claude.ai/code/artifact/5a994b1f-7c2a-423c-8361-d9a40a8f9312**

Kalau mau baca PRD lengkap, buka link di atas — jangan re-generate dari nol, artifact-nya masih ada dan sudah final untuk versi saat ini.

## 🔗 Repository

**https://github.com/FazriAhmad/play-arena.git** — seluruh isi folder ini (STATUS.md, logo, referensi-play-arena, Ui-PlayArena, Api-PlayArena) di-push sebagai satu repo, sama seperti pola LMS. `.env`/`vendor`/`node_modules` semua ter-exclude lewat `.gitignore` masing-masing folder.
- Branch `main` — backend Modul 01 (Api-PlayArena) selesai (snapshot lama, jauh tertinggal).
- Branch `dev` — **branch kerja saat ini, dan satu-satunya yang aktif dikembangkan**. Berisi seluruh Fase 1 (Modul 01–05, 07–10, Modul 06 ditunda) + Fase 2 yang sedang berjalan (Modul 11 selesai). **Keputusan user (2026-08-28): jangan merge ke `main` sampai diminta eksplisit** — sebelumnya alasannya "simpan di branch dev dulu", sekarang eksplisit "tetap di dev, lanjut ke fase selanjutnya". Jangan inisiatif merge sendiri.

## 🎨 Referensi &amp; Aset yang Sudah Dibuat User

- **`logo-rec-center.jpg`** — logo resmi PlayArena ("Unleash the Spirit"), palet biru (`#1d5fc4`) &amp; oranye. Sudah dipakai di PRD artifact (sidebar + cover).
- **`referensi-play-arena/`** — reference frontend React 19 + TypeScript + Vite + Tailwind v4 (mock data, belum tersambung backend). Berguna untuk tipe data (`src/lib/types.ts`: Venue, Court, Booking, Customer, Voucher, Staff, BlockSlot, dst.) dan komponen UI (SlotGrid, VenueCards, InvoiceSheet). **Cuma referensi tampilan/tipe data — bukan acuan tech stack.** Tech stack tetap sesuai Keputusan Kunci di bawah (Laravel 12 + PostgreSQL di backend), bukan ikut apa pun yang tersirat dari reference ini (reference murni frontend mock, tidak punya backend/payment/DB constraint sama sekali).
- **Tabel database `play_arena`** — sudah dibuat user langsung di PostgreSQL lokal (di luar repo, tidak ada file SQL yang di-commit).

## Keputusan Kunci (jangan diubah tanpa diskusi ulang)

- **Model bisnis: satu pemilik, multi-venue** — bukan marketplace terbuka (tidak ada alur approval vendor/komisi platform/payout ke banyak pemilik). Kalau ini berubah nanti, itu perubahan arsitektur besar (isolasi data per tenant), bukan modul tambahan biasa.
- **Fitur paling kritis: deteksi bentrok jadwal.** Dijamin lewat PostgreSQL `EXCLUDE USING gist` pada rentang waktu booking per lapangan — database yang menolak insert overlap, bukan cuma dicek di kode aplikasi. Ini alasan PostgreSQL dipertahankan sebagai database, bukan cuma ikut kebiasaan proyek lain.
- **Tidak pakai WebSocket/real-time push** untuk kalender — cukup refetch berkala, karena kebenaran data sudah dijamin di level database, bukan di real-time-nya.
- **Tech stack** disamakan dengan proyek lain: Laravel 12 + PostgreSQL (backend), React 19 + TypeScript + Vite + Tailwind (frontend).
- **Payment gateway: Midtrans, SANDBOX SAJA** (keputusan 2026-08-27) — proyek ini portofolio/demo, tidak akan deploy ke production sungguhan. Jangan bikin akun merchant terverifikasi atau proses go-live, cukup sandbox environment yang memang gratis tanpa verifikasi apa pun.
- **Konfirmasi booking: manual via WhatsApp click-to-chat (`wa.me`), BUKAN WhatsApp Business API** (keputusan 2026-08-27, revisi dari rencana awal pakai Cloud API resmi). Alur baru: booking dibuat → slot langsung ter-hold → status "Menunggu ACC Admin" → link `wa.me` ke nomor admin venue otomatis disiapkan/dibuka di sisi pelanggan (bukan server yang kirim, karena tanpa API server tidak bisa auto-kirim WA) → admin ACC/reject manual dari dashboard → **setelah ACC baru muncul pembayaran** (Midtrans). Kalau 10 menit belum di-ACC, tombol "Chat Admin via WhatsApp" muncul di halaman status booking pelanggan untuk follow-up manual (bisa ditekan berkali-kali). Reminder tetap otomatis tapi lewat Email (bukan WA, karena WA di sini murni manual). Upgrade ke WA Business API resmi dicatat sebagai langkah lanjutan kalau volume booking bikin ACC manual jadi bottleneck.
- **Form booking wajib isi nomor WA yang bisa dihubungi** (keputusan 2026-08-27) — dipakai admin buat kontak manual kalau perlu, BUKAN kanal reminder otomatis. Reminder tetap 2x lewat Email: **H-1 dan H-2jam** sebelum jadwal main (sebelumnya cuma H-1, ditambah H-2jam sesuai request). Alasan tetap Email bukan WA: reminder itu sistem yang harus proaktif kirim tanpa ada pelanggan yang menekan tombol — `wa.me` click-to-chat tidak bisa itu, butuh WA Business API asli (balik ke opsi berbayar yang sudah ditolak sebelumnya).
- **Promo &amp; pengumuman (Modul 16): tampil di web saja untuk sekarang** (keputusan 2026-08-27) — banner di beranda + daftar pengumuman di akun pelanggan begitu mereka login, TIDAK didorong keluar lewat WA/Email. Broadcast aktif (push ke semua pelanggan sekaligus) ditunda, bukan prioritas MVP.
- **Peta lokasi: Leaflet.js + OpenStreetMap** — gratis tanpa API key/billing, dipilih di atas Google Maps yang butuh akun billing aktif.
- **Tiga peran:** Owner/Admin (akses penuh semua venue), Staff/Kasir (terbatas ke venue yang ditugaskan), Pelanggan (publik).
- **Dependensi eksternal:** tidak ada — sudah tidak relevan sejak Midtrans diputuskan sandbox-only (lihat poin di atas).

## Struktur Folder

```
C:\Users\Fazri\portofolio\rec-center-book\
├── STATUS.md                  (file ini)
├── logo-rec-center.jpg         (logo resmi PlayArena)
├── referensi-play-arena/       (reference frontend React+Vite+TS, mock data — cuma UI/tipe, bukan acuan tech stack)
├── Ui-PlayArena/                (frontend production — Vite+React+TS+Tailwind v4, di-scaffold 2026-08-27)
└── Api-PlayArena/               (backend production — Laravel 12+PostgreSQL, di-scaffold 2026-08-27)
```

### Api-PlayArena (backend) — Modul 01 (Pengguna & Role) selesai, teruji end-to-end
- Laravel 12, terhubung ke database `play_arena` (Postgres lokal, `127.0.0.1:5432`, user `postgres`)
- Port dev: **8020** (bukan 8000/8010 — biar tidak rebutan port dengan project lain di portofolio ini, termasuk LMS yang pakai 8010). Jalankan manual: `php artisan serve --host=127.0.0.1 --port=8020`
- Package terpasang: `laravel/sanctum` (auth token), `spatie/laravel-permission` (role), `laravel/boost` (dev tooling — ada `CLAUDE.md`/`AGENTS.md` sendiri di folder ini berisi guideline khusus Laravel, dibaca otomatis tiap masuk ke folder ini)
- `User` model pakai `HasApiTokens` + `HasRoles`, kolom `phone` & `is_active` ditambah lewat migration. Relasi `venues()` (staff ↔ venue via `venue_staff`) dan `ownedVenues()` (owner → venue miliknya).
- Custom `Authenticate` middleware (return JSON 401, bukan redirect ke halaman login yang nggak ada) — pola yang sama dipakai di Api-LMS setelah bug serupa pernah kejadian di project money-management
- Tabel `venues` (skema minimal — CRUD lengkap fasilitas/foto/dst nyusul di Modul 03) & `venue_staff` (pivot penugasan) sudah ada, dibutuhkan Modul 01 untuk fitur "penugasan staff ke venue"
- **3 role di-seed** (`owner`, `staff`, `pelanggan`) + 1 akun owner bootstrap: `owner@playarena.test` / `owner12345` (`database/seeders/RoleSeeder.php`, dipanggil dari `DatabaseSeeder`)
- Endpoint jalan (`routes/api.php`): `POST /register` (pelanggan saja), `POST /login` (email atau no. HP), `POST /logout`, `GET /me`, `POST /forgot-password`, `POST /reset-password`, `GET /venues`, `POST /venues` (owner), `GET|POST /staff` + `PUT /staff/{id}` (owner — assign role staff + `venue_ids`)
- **2 bug ditemukan & diperbaiki saat testing end-to-end** (bukan cuma "tidak error", dicek nilai aktualnya):
  1. `is_active` balik `null` bukan `true` setelah register/create staff — bukan bug refresh-default seperti di LMS, tapi karena kolom itu belum masuk `#[Fillable(...)]` di `User` model sehingga mass-assignment-nya di-drop diam-diam. Kalau nemu pola serupa (field kekirim tapi hasilnya null), curiga dulu ke `Fillable` sebelum ke hal lain.
  2. `forgot-password` selalu 500 karena notifikasi reset password bawaan Laravel manggil `route('password.reset', ...)` — route web yang memang tidak ada di API murni ini. Diperbaiki dengan `ResetPassword::createUrlUsing()` di `AppServiceProvider` supaya link reset mengarah ke `FRONTEND_URL` (env baru, default `http://127.0.0.1:5180`), bukan ke route Laravel.
- **Sudah teruji end-to-end**: register pelanggan → login pakai email → login pakai nomor HP → owner login → owner buat venue → owner buat staff + assign venue → staff login → staff coba akses endpoint owner-only (403) → forgot-password → link di log mengarah ke frontend dengan token valid → reset-password → login pakai password baru berhasil → logout → token lama ditolak (401)

### Api-PlayArena — Modul 02 (Direktori & Pencarian Lapangan) selesai, teruji end-to-end
- Tabel `courts` baru (venue_id, name, sport, price_per_hour, photo_url, facilities JSON, is_active) + model `Court`, relasi `Venue::courts()`
- `GET /venues` (publik, tanpa login) — filter `sport`, `city` (ilike), `min_price`, `max_price`; tiap venue diringkas jadi `{cover, sports[], price_from, courts_count}` dari lapangan aktifnya
- `GET /venues/{venue}` (publik) — detail venue + daftar lapangan aktif
- `GET /venues/mine` (auth, wajib didaftarkan **sebelum** `/venues/{venue}` di routes/api.php supaya tidak ketangkep sebagai route-model-binding) — dipisah dari `/venues` publik karena `/venues` sekarang untuk direktori, bukan lagi dropdown "venue milik saya"
- `POST /venues/{venue}/courts` (owner, stub minimal — form pengelolaan lapangan lengkap nyusul Modul 03)
- **Breaking change internal**: StaffPage yang tadinya manggil `GET /venues` buat dropdown penugasan staff, sekarang manggil `GET /venues/mine` — kalau nemu kode lama yang masih pakai `/venues` buat itu, itu bug lama, ganti ke `/venues/mine`

### Ui-PlayArena — Modul 02 selesai, teruji end-to-end di browser
- `SearchPage` (`/`, publik) — filter olahraga/kota/rentang harga (debounced 300ms), grid kartu venue
- `VenueDetailPage` (`/venue/:id`, publik) — info venue, peta lokasi (Leaflet + tile OSM asli, bukan react-leaflet — cukup pakai Leaflet vanilla di `useEffect` karena cuma butuh satu marker statis), daftar lapangan + harga + fasilitas
- `VenueMap.tsx` — perlu override `L.Icon.Default.mergeOptions` supaya ikon marker Leaflet ke-resolve lewat asset Vite (default-nya reference path relatif yang patah di bundler)
- `AppLayout` diubah supaya mendukung guest (belum login) — sebelumnya `return null` kalau `user` kosong, sekarang tampilkan header dengan tombol Masuk/Daftar
- Routing: `/` dan `/venue/:id` publik (di luar `ProtectedRoute`), redirect pasca-login beda per role (pelanggan → `/`, owner/staff → `/dashboard`)
- **Sudah teruji end-to-end di browser sungguhan**: buka `/` tanpa login → 2 venue demo tampil → filter olahraga lewat dropdown asli (bukan curl) → hasil ke-filter benar → klik venue → peta Leaflet render dengan marker + attribution OSM → daftar lapangan & harga tampil benar, tanpa error console

### Api-PlayArena & Ui-PlayArena — Modul 03 (Kelola Data Lapangan) selesai, teruji end-to-end
- Backend: `VenueController::ownerIndex/ownerShow/update` (lihat &amp; kelola semua venue milik sendiri, termasuk yang nonaktif — beda dari `/venues` publik yang cuma tampilkan yang aktif), `CourtController::update/destroy` (lengkapi stub Modul 02) — semua di bawah `/owner/venues` &amp; `/owner/courts`, role `owner` saja. **Namespace ini di-rename ke `/manage/*` di Modul 04** (lihat bawah), catatan di sini dibiarkan sebagai jejak sejarah.
- Upload foto lapangan: `Storage::disk('public')`, endpoint update court pakai `POST` bukan `PUT` (PHP tidak parse file upload di request `PUT` multipart tanpa method-spoofing tambahan — daripada ribet, langsung `POST` saja)
- **Bug ditemukan &amp; diperbaiki**: kolom "cover" foto di direktori publik (`VenueController::summarize`) sempat ambil lapangan pertama tanpa urutan pasti dari DB — kalau lapangan yang kebetulan "pertama" belum ada fotonya padahal lapangan lain di venue sama sudah, cover ikut kosong. Diperbaiki: cari eksplisit lapangan aktif pertama yang **punya** foto, bukan sekadar lapangan pertama.
- Frontend: `ManageVenuesPage` (`/owner/venues` — daftar &amp; tambah venue), `ManageVenueDetailPage` (`/owner/venues/:id` — edit venue, toggle aktif venue, tambah lapangan dengan upload foto via `FormData`, toggle aktif/nonaktif &amp; hapus lapangan)
- **Sudah teruji end-to-end di browser sungguhan** (bukan cuma curl): login owner → buka Kelola Lapangan → masuk detail venue → nonaktifkan lapangan lewat tombol asli → status berubah jadi "Nonaktif" → aktifkan lagi → isi form Tambah Lapangan (nama, harga) → submit → lapangan baru muncul di daftar → hapus lapangan itu (lewat dialog konfirmasi) → hilang dari daftar — semua tanpa error console

### Api-PlayArena & Ui-PlayArena — Modul 04 (Kalender & Blokir Slot) + Modul 05 (Booking & Deteksi Bentrok) selesai, teruji end-to-end
- **Modul 05 = fitur paling kritis di seluruh PRD.** Migration `bookings`: `CREATE EXTENSION IF NOT EXISTS btree_gist` lalu `ALTER TABLE bookings ADD CONSTRAINT bookings_no_overlap EXCLUDE USING gist (court_id WITH =, tstzrange(starts_at, ends_at, '[)') WITH &&) WHERE (status IN ('menunggu_acc','menunggu_bayar','confirmed'))` — PostgreSQL sendiri yang menolak insert overlap, bukan cuma dicek di kode aplikasi. Diverifikasi lewat `pg_constraint` langsung.
- `blocked_slots` (per lapangan, dibuat staff/owner venue terkait) + `booking_hold_minutes` di `venues` (default 60) — batas waktu booking "menunggu_acc" sebelum otomatis dibatalkan.
- **Namespace route owner di-rename `/owner/*` → `/manage/*`** (shared, dipakai owner & staff dengan level otorisasi beda lewat trait baru `AuthorizesVenue`: `authorizeOwner` — CRUD venue/lapangan tetap owner-only; `authorizeVenueStaff` — lihat + kelola blocked-slots, owner atau staff yang ditugaskan). Kalau nemu kode/dokumentasi lama yang masih sebut `/owner/venues` atau `/owner/courts`, itu nama lama — endpoint sekarang di `/manage/venues`, `/manage/courts/{court}/blocked-slots`, dst. `VenueController::ownerIndex/ownerShow` juga sudah di-rename jadi `manageIndex/manageShow`.
- `GET /courts/{court}/slots?date=` (publik) — grid per-jam status `past/blocked/booked/available`, dipakai baik di booking pelanggan maupun kalender admin.
- `POST /courts/{court}/bookings` (auth) — validasi jam operasional venue &amp; blocked_slots dulu (UX, pencegahan dini), lalu insert; kalau tetap kena race dan constraint DB yang menolak (SQLSTATE `23P01`), ditangkap jadi pesan "Slot ini baru saja diambil orang lain."
- `bookings:expire-stale` (Artisan command) + `Schedule::command(...)->everyFiveMinutes()` di `routes/console.php` — auto-cancel booking `menunggu_acc` yang lewat `booking_hold_minutes` venue-nya.
- **Bug ditemukan &amp; diperbaiki**: `config/app.php` timezone hardcoded `'UTC'` (bukan baca `env('APP_TIMEZONE')`) — sama persis kelas bug yang pernah kejadian di project LMS. Booking sempat tersimpan meleset 7 jam dari WIB sebelum diperbaiki.
- **Bug ditemukan &amp; diperbaiki**: `BookingController::mine()` eager-load kolom `court` tidak menyertakan `price_per_hour` → frontend nampilin "RpNaN/jam" di halaman Booking Saya. Diperbaiki dengan menambahkan kolom itu ke `with()`.
- Frontend: `SlotGrid` (reusable) dipakai di dua tempat — `BookingPanel` (interaktif, pelanggan pilih jam) di `VenueDetailPage`, dan `BlockManager` (read-only + form blokir) per lapangan di `ManageVenueDetailPage`. `MyBookingsPage` (`/bookings`) — riwayat booking pelanggan dengan status badge. `RoleRoute` sekarang terima `Role | Role[]` supaya `/manage/venues*` bisa dipakai owner maupun staff sekaligus.
- **Sudah teruji end-to-end lewat curl DAN browser sungguhan** (bukan cuma "tidak error", nilai aktual dicek):
  1. Booking overlap ditolak, booking berdampingan (back-to-back, batas jam sama) diterima, slot terlepas lagi setelah booking dibatalkan/expired — dicek langsung ke constraint.
  2. Staff cuma bisa akses venue yang ditugaskan (403 di venue lain).
  3. `bookings:expire-stale` membatalkan booking `menunggu_acc` yang lewat batas waktu venue (dites dengan `booking_hold_minutes=0`).
  4. Login sebagai pelanggan kedua, coba booking 13:00–15:00 (2 jam) yang overlap booking pelanggan pertama di 14:00 — client-side grid cuma nge-disable jam mulai yang sudah kepakai, tidak bisa deteksi overlap durasi multi-jam ini sendiri, tapi server menolak dan browser menampilkan pesan error yang benar. Membuktikan prinsip "server sumber kebenaran, client cuma UX" di PRD benar-benar jalan.
  5. Login sebagai owner, buka `/manage/venues/1`, expand "Kalender & Blokir Slot" salah satu lapangan, isi form blokir lewat UI asli (bukan curl) → slot di grid langsung berubah jadi label alasan blokir → hapus blokir lewat tombol Hapus → grid balik jadi "Tersedia" — semua lewat interaksi UI nyata.

### Api-PlayArena & Ui-PlayArena — Modul 07 (Kelola Booking Masuk) selesai, teruji end-to-end. **Modul 06 (Pembayaran Midtrans) ditunda**
- **Modul 06 ditunda atas keputusan user (2026-08-27)**: butuh Server Key & Client Key dari akun Midtrans Sandbox yang cuma bisa didaftarkan user sendiri (saya tidak bisa membuat akun atas nama user — kebijakan). User pilih lanjut Modul 07 dulu daripada bangun integrasi Midtrans tanpa key nyata.
- Modul 07 tetap menghasilkan alur "confirmed" yang benar-benar berfungsi tanpa Midtrans, lewat jalur **konfirmasi pembayaran manual** yang memang sudah ada di PRD (transfer langsung ke rekening venue, di luar Midtrans) — bukan versi separuh-jadi.
- Migration: `pelanggan_id` di `bookings` diubah jadi nullable (raw `ALTER COLUMN ... DROP NOT NULL`, sengaja tidak pakai `->change()` supaya tidak butuh doctrine/dbal), + kolom `guest_name`, `reject_reason`, `created_by` — dipakai booking walk-in yang tidak punya akun pelanggan. Tabel `payments` baru (`booking_id`, `method`, `amount`, `status`, `reference` nullable buat referensi Midtrans nanti, `confirmed_by`, `confirmed_at`).
- **Trait `CreatesBookings`** — logika validasi jam operasional + cek `blocked_slots` + tangkap SQLSTATE `23P01` dari exclusion constraint diekstrak dari `BookingController::store` (Modul 05) supaya dipakai ulang persis sama oleh jalur booking walk-in staff, bukan disalin dua kali. Kalau nemu bug di validasi booking nanti, cek di sini — satu tempat untuk dua jalur (online & walk-in).
- `ManageBookingController`: `index` (filter status/tanggal/lapangan, scoped ke venue yang dikelola user login — pola yang sama seperti `manageIndex` Modul 04), `accept` (`menunggu_acc`→`menunggu_bayar`), `reject` (wajib `reason`, →`rejected`), `confirmPayment` (`menunggu_bayar`→`confirmed` + catat `Payment` `method=manual`), `walkIn` (booking langsung `confirmed` + `Payment` `method=manual`, tetap lewat exclusion constraint yang sama seperti booking online — bukan jalur pintas). Semua endpoint scoped lewat `AuthorizesVenue::authorizeVenueStaff` (owner ATAU staff venue terkait).
- Frontend: `ManageBookingsPage` (`/manage/bookings`) — daftar booking + filter status/tanggal, tombol ACC/Tolak (reject minta alasan via `prompt()`) untuk `menunggu_acc`, tombol Konfirmasi Pembayaran untuk `menunggu_bayar`, form Booking Walk-in (pilih venue → dropdown lapangan reload otomatis, tanggal/jam/durasi, nama & WA tamu). Nav "Booking Masuk" ditambah untuk owner & staff.
- **Catatan testing browser**: tombol Konfirmasi Pembayaran &amp; Tolak pakai `window.confirm()`/`window.prompt()` bawaan browser — dialog native ini **memblokir automation tool** (klik terdaftar tapi request tidak pernah terkirim sampai dialog di-resolve). Solusinya override `window.confirm`/`window.prompt` lewat `javascript_tool` sebelum klik (bukan bug aplikasi, cuma keterbatasan automation). Kalau nemu tombol serupa nanti yang "tidak bereaksi" pas ditest browser, curiga dulu ke pola ini sebelum curiga ke kode.
- **Sudah teruji end-to-end lewat curl DAN browser sungguhan**:
  1. Alur penuh: booking pelanggan → ACC (`menunggu_bayar`) → konfirmasi bayar manual (`confirmed`) → tercermin benar di halaman Booking Saya pelanggan (dicek dari sisi akun pelanggan asli, bukan cuma respons API admin).
  2. Reject tanpa alasan ditolak (422 lewat curl); reject dengan alasan tersimpan &amp; tampil ke pelanggan (dites lewat browser, `prompt()` di-override balikin "test", tersimpan &amp; tampil "Alasan: test").
  3. Booking yang sudah diproses tidak bisa di-ACC/reject/confirm-payment ulang (422 guard status, dicek lewat curl).
  4. Staff venue lain (assigned ke venue 1) tidak bisa ACC/reject booking venue lain (403), dan booking venue itu tidak muncul di listing `/manage/bookings` miliknya — dites dengan booking lintas-venue asli, bukan asumsi.
  5. Walk-in: booking langsung `confirmed` + `payments` row dengan `amount` benar (`price_per_hour × duration_hours`, dicek 175.000×2=350.000), tetap tunduk exclusion constraint. Dites juga lewat browser sungguhan: pilih venue → dropdown lapangan reload otomatis → submit → booking baru langsung muncul "Terkonfirmasi" di daftar.
  6. Filter status di `ManageBookingsPage` dites lewat dropdown asli — hasil ke-filter benar (cuma tampilkan booking `confirmed` saat filter dipilih).

### Api-PlayArena & Ui-PlayArena — Modul 08 (Riwayat & Invoice) selesai, teruji end-to-end
- `BookingController::show` (`GET /bookings/:id`) — detail satu booking (venue+alamat, lapangan, jadwal, riwayat `payments`), scoped ketat ke pelanggan pemiliknya (403 kalau bukan miliknya, dites dengan akun owner coba lihat booking pelanggan lain).
- **`bookings:mark-completed`** (Artisan command + scheduler `everyFiveMinutes`) — booking `confirmed` yang jam mainnya sudah lewat otomatis ditandai `completed`. Tanpa ini status booking bakal nyangkut selamanya di `confirmed` walau sudah lewat, dan pengelompokan riwayat pelanggan (akan datang/selesai/dibatalkan) jadi salah, plus Modul 13 (Rating & Review, nanti) butuh status `completed` yang bisa diandalkan buat gerbang boleh-review.
- **Keputusan invoice PDF (2026-08-27)**: tidak menambah dependency PDF baru di backend (mis. `barryvdh/laravel-dompdf`) — proyek pakai aturan "dependency baru butuh approval", dan user pilih pendekatan lebih ringan: halaman invoice HTML rapi di frontend + tombol "Cetak / Simpan Invoice PDF" yang panggil `window.print()` browser (pelanggan pilih "Simpan sebagai PDF" di dialog print). Kalau nanti mau PDF asli digenerate di server, ini keputusan yang perlu direvisit, bukan default.
- Frontend: `MyBookingsPage` dikelompokkan jadi 3 tab (Akan Datang/Selesai/Dibatalkan) — bucket dihitung dari `status` (rejected/cancelled → Dibatalkan, completed → Selesai, sisanya → Akan Datang), tiap kartu link ke `BookingDetailPage` (`/bookings/:id`) baru — nomor invoice (`INV-PA-{id}`), rincian harga, metode &amp; waktu bayar, kontak. Invoice (nomor &amp; tombol cetak) cuma muncul untuk booking `confirmed`/`completed`; booking `rejected` tetap tampil detail lengkap + alasan penolakan, tanpa elemen invoice. `print:hidden` dipakai di header `AppLayout` &amp; tombol aksi supaya hasil cetak cuma nampilkan kartu invoice, bukan seluruh halaman.
- **Sudah teruji end-to-end lewat curl DAN browser sungguhan**: akses booking pelanggan lain ditolak (403); `bookings:mark-completed` membalik status booking lampau jadi `completed` (dites dengan booking waktu lampau via tinker); di browser — tab Akan Datang/Selesai/Dibatalkan menampilkan booking yang benar sesuai bucket-nya, halaman detail booking confirmed menampilkan invoice lengkap dengan data akurat (harga × jam = total benar), halaman detail booking rejected menampilkan alasan penolakan tanpa elemen invoice — tanpa error console.

### Api-PlayArena & Ui-PlayArena — Modul 09 (Cancel, Reschedule & Refund) selesai, teruji end-to-end
- `venues.full_refund_hours` (default 24) — kebijakan refund berjenjang per venue: batal ≥ sekian jam sebelum jadwal main = refund penuh, kurang dari itu = pembayaran hangus. `bookings.cancel_reason` (field baru, terpisah dari `reject_reason` yang khusus penolakan ACC). Tabel `refunds` baru (`booking_id`, `amount`, `status` entitled/forfeited/processed, `reason`, `processed_by`, `processed_at`) — dibuat HANYA kalau booking yang dibatalkan sudah pernah dibayar (`payments` status `paid` > 0); kalau belum pernah bayar, cancel tidak menghasilkan row refund sama sekali (tidak ada yang perlu dikembalikan).
- **Trait `CancelsBookings`** — logika kebijakan refund (hitung selisih jam ke `starts_at`, tentukan entitled/forfeited) dipakai bersama oleh pembatalan pelanggan (`BookingController::cancel`, alasan opsional) dan Staff/Owner (`ManageBookingController::cancel`, alasan **wajib**) supaya aturannya konsisten persis di dua jalur.
- `BookingController::reschedule` — pindah booking ke slot lain, tunduk PENUH pada `CreatesBookings` (jam operasional, blocked_slots, exclusion constraint — "tidak ada jalur pintas" sesuai PRD), dibungkus `DB::transaction` supaya booking lama TIDAK ikut batal kalau slot baru gagal (bentrok/diblokir) — diverifikasi lewat percobaan reschedule ke slot yang sengaja dibentrokkan, booking lama tetap utuh setelah 422.
- **Keputusan simplifikasi (2026-08-27)**: reschedule cuma diperbolehkan untuk booking yang BELUM dibayar (`menunggu_acc`/`menunggu_bayar`). Booking `confirmed` (sudah dibayar) harus dibatalkan dulu (tunduk kebijakan refund di atas) baru booking ulang manual — PRD tidak menyebutkan cara memindahkan pembayaran yang sudah lunas ke slot baru, jadi tidak diasumsikan/dibangun sendiri. Kalau nanti ada permintaan reschedule booking yang sudah dibayar, ini keputusan yang perlu direvisit bareng user, bukan diselesaikan sepihak.
- Frontend: `BookingDetailPage` — tombol "Batalkan Booking" (booking aktif apa pun) & "Jadwal Ulang" (cuma kalau belum dibayar) dengan form reschedule yang reuse `SlotGrid` persis seperti alur booking baru; tampilan alasan pembatalan &amp; status refund untuk booking `cancelled`. `ManageBookingsPage` — tombol "Batalkan" (alasan wajib lewat `prompt()`) + tampilan yang sama.
- **Bug ditemukan &amp; diperbaiki sebelum verifikasi selesai**: setelah reschedule me-`navigate()` ke booking baru, form reschedule booking LAMA tetap kebuka — React Router tidak remount komponen halaman waktu cuma parameter `:id` di URL yang berubah, jadi state lokal (`showReschedule`) ikut kebawa. Diperbaiki dengan reset state itu di dalam `useEffect` yang bergantung ke `id`. Kalau nemu state "nyangkut" serupa di halaman lain yang punya route dinamis (`/*/:id`), curiga ke pola ini duluan.
- **Sudah teruji end-to-end lewat curl DAN browser sungguhan**: cancel booking belum dibayar → tidak ada refund row; cancel booking `confirmed`+lunas ≥24 jam sebelum jadwal → refund `entitled` sejumlah yang dibayar; cancel &lt;24 jam sebelum jadwal → refund `forfeited` (amount 0) — dites dengan booking waktu nyata (5 jam lagi vs beberapa hari lagi). Reschedule berhasil pindah slot (booking lama otomatis `cancelled`, booking baru `menunggu_acc`); reschedule ke slot bentrok ditolak (422) DAN booking lama terbukti tidak ikut batal (transaction rollback). Staff cancel tanpa alasan ditolak (422), dengan alasan berhasil dan refund dihitung sama. Browser: form reschedule (pilih tanggal → grid reload otomatis → pilih jam → submit) berhasil navigasi ke halaman booking baru dengan form lama sudah tertutup (setelah fix), alasan &amp; status refund tampil benar di halaman detail pelanggan maupun daftar admin.
- **Catatan testing browser**: tombol Batalkan pakai `window.prompt()`/`confirm()` bawaan seperti Modul 07 — override lewat `javascript_tool` kadang tidak "nempel" persis di detik klik terjadi (`prompt() is not supported` dari environment automation), jadi verifikasi tombol "Batalkan" sisi Staff/Owner mengandalkan curl (dites tuntas: guard alasan wajib, kalkulasi refund) + pengecekan visual tombol muncul/hilang sesuai status lewat `read_page`, bukan klik-tuntas di browser untuk kasus itu spesifik.

### Api-PlayArena & Ui-PlayArena — Modul 10 (Konfirmasi Admin via WhatsApp + Reminder Email) selesai, teruji end-to-end — **modul terakhir Fase 1**
- `venues.admin_wa` (nomor WA admin per venue) — dasar link `wa.me` click-to-chat, diisi owner lewat form edit venue (Modul 03), diekspos di direktori publik (`GET /venues/:id`) karena dibutuhkan SEBELUM booking dibuat (langsung setelah submit, bukan setelah reload).
- Tabel `notification_logs` (booking_id, channel, type `reminder_h1`/`reminder_h2jam`, status, sent_at) — mencegah reminder terkirim dobel walau `SendBookingReminders` tertangkap di beberapa ketukan cron sekaligus (jendela waktu sengaja dilebarkan ±15 menit dari target H-24/H-2 biar tidak kelewat kalau scheduler telat, dedup-nya yang menjamin tidak dobel, bukan presisi jendela waktu).
- `BookingReminderMail` (markdown mail) — dites pakai `MAIL_MAILER=log` (sudah default di `.env` proyek ini), isi email diverifikasi akurat lewat `storage/logs/laravel.log` tanpa perlu SMTP sungguhan.
- **Keputusan PRD yang dipegang teguh**: WA di alur ini murni manual click-to-chat (`wa.me`), BUKAN WhatsApp Business API — makanya reminder terjadwal (H-1 &amp; H-2jam) cuma lewat Email, bukan WA. Booking walk-in (`pelanggan_id` null) otomatis dilewati `SendBookingReminders` karena tidak ada akun/email untuk dikirimi.
- Frontend: `src/lib/whatsapp.ts` (`buildWaLink` — normalisasi nomor lokal `08xx` → `62xx`, `buildBookingWaMessage`) dipakai di dua tempat: (1) `VenueDetailPage` — begitu booking berhasil dibuat, link `wa.me` otomatis `window.open()` ke tab baru + tombol "Kirim Konfirmasi via WhatsApp" tetap tampil sebagai fallback kalau popup diblokir; (2) `BookingDetailPage` — tombol "Chat Admin via WhatsApp" muncul otomatis kalau booking masih `menunggu_acc` DAN sudah ≥10 menit sejak `created_at`, bisa ditekan berkali-kali untuk follow-up manual sesuai PRD.
- `ManageVenueDetailPage` (`VenueEditForm`) — field baru "Nomor WA Admin" supaya owner bisa isi/ubah nomor per venue.
- **Sudah teruji end-to-end lewat curl, tinker, DAN browser sungguhan**: `bookings:send-reminders` mengirim reminder H-1 &amp; H-2jam yang benar dengan booking waktu nyata (24 jam &amp; 2 jam ke depan), isi email (nama pelanggan, venue, lapangan, tanggal/jam) akurat dicek langsung dari log file; dijalankan kedua kali tidak kirim ulang (dedup terbukti bekerja, 0 terkirim). Booking baru dari `VenueDetailPage` menghasilkan link `wa.me` dengan nomor admin ternormalisasi benar (`081234500001` → `6281234500001`) dan pesan URL-encoded utuh (nama, lapangan, tanggal, jam, `#id` booking). Booking berumur 15 menit yang masih `menunggu_acc` menampilkan tombol follow-up; booking baru (&lt;10 menit) tidak menampilkannya — dites dengan dua booking nyata, bukan asumsi. Form "Nomor WA Admin" berhasil simpan &amp; termuat balik lewat form asli.
- **Dengan Modul 10 selesai, seluruh Fase 1 (MVP) tuntas kecuali Modul 06 (Pembayaran Midtrans) yang masih ditunda** menunggu user mendaftar Midtrans Sandbox sendiri (lihat catatan Modul 06 di atas). Modul 07 sudah menyediakan jalur pembayaran manual yang berfungsi penuh sebagai pengganti sementara.

### Api-PlayArena & Ui-PlayArena — Modul 11 (Booking Berulang) selesai, teruji end-to-end — **awal Fase 2**
- **Keputusan user (2026-08-28)**: tetap di branch `dev`, TIDAK merge ke `main` dulu meski Fase 1 sudah tuntas — lanjut langsung ke Fase 2. Jangan merge ke `main` sampai user minta eksplisit.
- Tabel `recurring_groups` (court, pelanggan, `day_of_week` 0=Minggu..6=Sabtu konvensi Carbon, `start_hour`, `duration_hours`, `starts_on`, `ends_on` nullable, `session_count` nullable — salah satu dari `ends_on`/`session_count` wajib sesuai `mode`). Kolom `recurring_group_id` nullable di `bookings` (`nullOnDelete` — belum ada fitur hapus grup, sengaja tidak dibangun karena tidak diminta PRD).
- `RecurringBookingController::store` — generate tanggal dari hari terpilih + tanggal mulai (roll maju ke kejadian hari itu berikutnya), lalu step mingguan sampai `session_count` atau `ends_on` tercapai (dibatasi keras 52 sesi sebagai pengaman terhadap abuse). **Tiap sesi dibuat lewat trait `CreatesBookings` yang sama persis dengan Modul 05/07** — tunduk penuh jam operasional, `blocked_slots`, dan exclusion constraint database, PERSIS sesuai PRD "tidak ada jalur pintas". Sesi yang bentrok ditangkap per-sesi (`HttpException` dari `abort()` di dalam trait) dan dilaporkan gagal di response — TIDAK menghentikan pembuatan sesi lain dalam rangkaian.
- Endpoint `POST /courts/{court}/recurring-bookings`, sama seperti Modul 05 terbuka untuk siapa pun yang login (tidak dibatasi `role:pelanggan`).
- Frontend: `VenueDetailPage` — tombol "Booking Berulang" baru di samping "Booking Sekarang" per lapangan, `RecurringBookingPanel` (pilih hari, jam mulai, durasi, tanggal mulai, radio "jumlah sesi" atau "sampai tanggal"). Submit menampilkan ringkasan hasil: jumlah sesi berhasil + daftar sesi gagal (tanggal &amp; alasan). Booking individual dari rangkaian otomatis muncul di `MyBookingsPage` (Modul 08) tanpa perubahan tambahan — tiap sesi memang cuma booking biasa dengan `recurring_group_id` terisi.
- **Catatan testing yang perlu diingat**: sempat curiga bug (satu request recurring-booking me-return `created: []` + semua sesi `failed` padahal 2 dari 3 tanggal seharusnya bebas konflik) — ternyata itu kesalahan skrip test saya sendiri: perintah bash pakai `curl ... | python3 -m json.tool || curl ...` dan `python3 -m json.tool` gagal (stub WindowsApps), memicu fallback `||` yang mengirim ulang POST yang SAMA sehingga request kedua bentrok dengan hasil request pertama yang sukses. Bukan bug aplikasi — dikonfirmasi dengan re-test bersih (satu curl call, tanpa pipe fallback) yang hasilnya benar. Pelajaran: hindari pola `curl | tool_yang_mungkin_tidak_ada || curl` saat test API, karena fallback shell bisa mengeksekusi ulang request yang punya efek samping (POST/create).
- **Sudah teruji end-to-end lewat curl DAN browser sungguhan**: mode `session_count` &amp; `until_date` menghasilkan tanggal yang benar (hari-of-week &amp; interval mingguan akurat, dicek manual satu per satu). Sesi yang bentrok dengan booking lain dilaporkan gagal dengan pesan jelas, sesi lain dalam rangkaian tetap berhasil dibuat — dites dengan booking konflik yang sengaja dibuat di tengah rangkaian (2 berhasil, 1 gagal, keduanya kasus curl dan browser). Validasi mode ditolak 422 kalau field yang relevan (`ends_on`/`session_count`) tidak diisi. Browser: form berhasil submit, ringkasan "X sesi berhasil, Y sesi gagal" tampil dengan daftar tanggal &amp; alasan gagal yang akurat; semua sesi yang berhasil muncul sebagai booking individual terpisah di Booking Saya.

### Api-PlayArena & Ui-PlayArena — Modul 13 (Rating & Review) selesai, teruji end-to-end. **Modul 12 (Split Payment) ditunda**
- **Modul 12 ditunda (2026-08-28)**: inti fiturnya "tiap peserta bayar sendiri via Midtrans" &amp; "booking confirmed setelah seluruh bagian lunas" bergantung penuh pada Midtrans, sama seperti Modul 06. Lompat ke Modul 13 dulu — jangan lupa isi Modul 12 begitu Midtrans sandbox key tersedia, sekalian dengan Modul 06.
- Tabel `reviews` (`court_id`, `pelanggan_id`, `booking_id` UNIQUE — satu booking selesai = satu bukti pernah main = maksimal satu review per booking, bukan per pelanggan-lapangan; pelanggan yang sama boleh review lagi kalau punya booking selesai lain di lapangan yang sama, `rating` 1-5, `comment` nullable).
- `ReviewController::store` — cuma pemilik booking dengan status `completed` yang boleh review (403 kalau bukan booking sendiri, 422 kalau belum selesai atau sudah pernah direview — guard `booking->review()->exists()`). `index()` — daftar ulasan publik per lapangan.
- `VenueController`: `courts` di `index()` &amp; `show()` sekarang pakai `withAvg('reviews','rating')` + `withCount('reviews')`; `summarize()` menghitung `rating_avg` &amp; `reviews_count` level VENUE (weighted average dari semua lapangan aktifnya, bukan rata-rata dari rata-rata) untuk kartu direktori (Modul 02).
- **Bug ditemukan &amp; diperbaiki sebelum verifikasi selesai**: `reviews_avg_rating` dari `withAvg()` balik sebagai STRING desimal Postgres (`"5.0000000000000000"`), bukan number — bakal bikin `.toFixed()` di frontend crash. Diperbaiki dengan cast eksplisit `'reviews_avg_rating' => 'float'` di `Court` model. Kalau nemu kolom hasil `withAvg`/`withSum` lain yang perlu dipakai numerik di frontend, ingat pola ini duluan.
- `BookingController::show()` eager-load relasi `review` supaya halaman detail booking tahu apakah booking itu sudah direview.
- Frontend: komponen `Stars` baru di `ui.tsx` (read-only atau interaktif untuk form, dipakai 3 tempat). `SearchPage` — badge rating di kartu direktori (venue tanpa ulasan tidak menampilkan badge sama sekali, bukan "0.0"). `VenueDetailPage` — badge rating per lapangan yang bisa diklik untuk membuka daftar ulasan (`GET /courts/:id/reviews`). `BookingDetailPage` — `ReviewSection`: booking `completed` yang belum direview menampilkan form (pilih bintang + komentar opsional), yang sudah direview menampilkan ulasannya read-only.
- **Sudah teruji end-to-end lewat curl DAN browser sungguhan**: review ditolak untuk booking bukan milik sendiri (403), booking belum selesai (422), dan booking yang sudah direview (422 — dites submit dua kali). Rating agregat venue &amp; lapangan akurat dicek langsung dari respons API (5.0 dari 1 ulasan pada Lapangan A, lalu venue jadi 4.5 gabungan setelah Lapangan B dapat ulasan 4.0). Browser: direktori menampilkan badge rating yang benar (venue dengan ulasan vs tanpa ulasan sama sekali), halaman venue menampilkan daftar ulasan asli (nama, bintang, komentar, tanggal) setelah tombol rating diklik, form review di halaman detail booking berhasil submit dan langsung berubah jadi tampilan read-only — tanpa error console baru.

### Api-PlayArena & Ui-PlayArena — Modul 14 (Voucher & Kode Promo) selesai, teruji end-to-end
- Tabel `promos` (`owner_id`, `venue_id` nullable = berlaku semua venue milik owner, `code` UNIQUE — dinormalisasi UPPERCASE sebelum divalidasi unique DAN disimpan supaya `"summer20"` &amp; `"SUMMER20"` tidak bisa jadi dua kode berbeda tanpa sengaja, `discount_type` percent/fixed, `value`, `min_amount` nullable, `quota` nullable = tanpa batas, `used_count`, `starts_at`/`ends_at`, `is_active`). Kolom `promo_code` &amp; `discount_amount` ditambahkan ke `bookings` — diisi sekali saat booking dibuat, dipakai ulang saat konfirmasi pembayaran (Modul 07) supaya nominal yang tercatat di `payments` sudah dikurangi diskon, bukan harga penuh.
- **Trait `AppliesPromoCode::resolvePromo`** — validasi lengkap (kode ada &amp; aktif, dalam periode berlaku, kuota belum habis, cakupan venue cocok, subtotal ≥ syarat minimum) dipakai bersama oleh preview publik (`PromoController::preview`, dicek pelanggan sebelum submit) dan penerapan sungguhan (`BookingController::store`) supaya aturannya konsisten di dua tempat.
- **Keputusan urutan operasi**: booking dibuat DULU (bisa gagal kena exclusion constraint Modul 05), kuota voucher (`used_count`) baru ditambah SETELAH booking benar-benar berhasil — supaya percobaan booking yang gagal bentrok tidak ikut menghabiskan kuota voucher orang lain. Reschedule (Modul 09) membawa `promo_code`/`discount_amount` yang sama ke booking baru (bukan transaksi baru, cuma pindah jadwal) — tidak perlu apply ulang.
- `PromoController` — CRUD Owner-only (`role:owner`, keputusan pemasaran — beda dari Modul 04 yang staff juga boleh kelola kalender) + `preview()` publik supaya pelanggan bisa cek voucher sebelum booking, bukan cuma pas submit.
- Frontend: `ManagePromosPage` (`/manage/promos`, link nav "Voucher" cuma muncul untuk owner) — daftar &amp; buat voucher, tampilkan kuota terpakai (`X/Y` atau "tanpa batas"), toggle aktif/nonaktif, hapus. `VenueDetailPage` (`BookingPanel`) — field "Kode Voucher (opsional)" + tombol "Terapkan" yang preview diskon real-time sebelum submit, rincian harga (harga asli/diskon/total) muncul begitu jam dipilih. `BookingDetailPage` — baris "Diskon voucher {kode}" di rincian harga invoice kalau booking pakai voucher.
- **Sudah teruji end-to-end lewat curl DAN browser sungguhan**: diskon persen &amp; nominal terhitung benar (150.000×20%=30.000; fixed di-cap tidak melebihi subtotal); syarat minimum transaksi ditolak dengan pesan jelas kalau subtotal kurang (dites 1 jam vs 2 jam pada voucher yang sama); cakupan venue ditegakkan (kode khusus satu venue "tidak ditemukan" di venue lain — pesan generik disengaja, tidak bocorkan bahwa kode itu sebenarnya ada tapi salah venue); kuota bertambah cuma setelah booking sukses, kuota habis (2/2) menolak percobaan ketiga (422); kode duplikat (case-insensitive) ditolak saat dibuat owner. Payment yang tercatat di `confirmPayment` sudah dikurangi diskon (150.000-30.000=120.000, dicek langsung ke tabel `payments`, bukan cuma respons API). Browser: form booking menampilkan rincian harga real-time setelah voucher diterapkan (termasuk kode yang di-uppercase otomatis saat mengetik), kode yang tidak ada menampilkan pesan error tanpa mengubah harga, halaman detail booking &amp; halaman Kelola Voucher menampilkan data yang akurat lewat form asli.

### Api-PlayArena & Ui-PlayArena — Modul 15 (Kelola Pelanggan/Member) selesai, teruji end-to-end
- Kolom `is_member` (boolean, default false) di `users` — prasyarat harga khusus member di Modul 21 (Fase 3), belum dipakai apa pun di luar penanda di modul ini. `User::bookingsAsCustomer()` — relasi baru (hasMany `Booking`, `pelanggan_id`) supaya bisa query pelanggan dari sisi `User`.
- `CustomerController` — daftar HANYA pelanggan yang pernah booking di venue milik owner login (scoped lewat `ownedVenues`, pola yang sama seperti Modul 07/09), BUKAN seluruh pelanggan platform. `index()` menghitung `bookings_count`, `total_spent` (jumlah `payments` status `paid`), `last_booking_at` per pelanggan. `show()` menambahkan riwayat booking lengkap (tetap scoped ke venue yang sama). `update()` toggle `is_member`. Semua endpoint menolak 404 kalau target user bukan pelanggan dari venue owner ini — dicek eksplisit, bukan cuma disembunyikan dari listing (dites: owner mencoba akses ID dirinya sendiri via endpoint pelanggan → 404).
- Route Owner-only (`role:owner`) — keputusan pemasaran/kepemilikan basis pelanggan, bukan operasional harian Staff.
- Frontend: `ManageCustomersPage` (`/manage/customers`, nav "Pelanggan" cuma muncul untuk owner) — daftar pelanggan dengan badge Member, statistik ringkas, tombol toggle member, "Lihat Riwayat" yang expand riwayat booking lengkap in-place (fetch on-demand per pelanggan, bukan langsung dimuat semua di `index()`).
- **Sudah teruji end-to-end lewat curl DAN browser sungguhan**: listing cuma menampilkan pelanggan yang benar-benar pernah booking di venue owner (owner sendiri hilang dari listing &amp; 404 kalau diakses langsung). `total_spent` &amp; `bookings_count` akurat dicek dari data booking/payment yang sudah ada. Toggle member bekerja dua arah, tercermin langsung di badge &amp; label tombol. Browser: statistik benar, "Lihat Riwayat" memuat seluruh riwayat booking dengan status badge akurat, toggle member lewat tombol asli berhasil dan langsung terlihat — tanpa error console baru.

### Api-PlayArena & Ui-PlayArena — Modul 16 (Promo & Pengumuman, web saja) selesai, teruji end-to-end
- Tabel `announcements` (`owner_id`, `title`, `body`, `target_segment` all/venue/member, `venue_id` nullable wajib kalau `target_segment=venue`, `is_active`). `User::announcements()` relasi baru.
- **Pola baru: endpoint publik dengan personalisasi opsional.** `AnnouncementController::active()` (`GET /announcements`) TIDAK pakai middleware `auth:sanctum` (yang akan menolak tamu dengan 401) — user diresolusi manual lewat `auth('sanctum')->user()` langsung di controller, balik `null` kalau tidak ada/token tidak valid. Segmen `all` selalu tampil ke siapa pun; `member` cuma untuk `is_member=true`; `venue` cuma untuk pelanggan yang pernah booking di venue itu (dicek via query `Booking`, bukan asumsi). Kalau nemu kebutuhan serupa nanti (endpoint yang harus tetap bisa diakses tamu TAPI personalized untuk yang login), pola ini yang dipakai — bukan bikin dua endpoint terpisah.
- CRUD Owner-only (`role:owner`) untuk kelola pengumuman — `venue_id` divalidasi milik owner yang sama seperti Modul 03/14.
- **Keputusan PRD yang dipegang teguh**: web saja untuk sekarang — TIDAK ada broadcast aktif lewat WA/Email (beda dari Modul 10 yang memang WA manual click-to-chat per booking, bukan broadcast). Pelanggan cuma lihat kalau mereka buka web sendiri, sesuai batasan &amp; non-goal di PRD.
- Frontend: `ManageAnnouncementsPage` (`/manage/announcements`, Owner-only) — daftar + form buat pengumuman, toggle aktif/nonaktif, hapus. `SearchPage` — banner pengumuman prioritas tertinggi di atas beranda, otomatis menyesuaikan viewer (tamu cuma lihat "semua pelanggan"). `AnnouncementsPage` (`/announcements`) baru — daftar pengumuman lengkap untuk akun pelanggan yang login, sesuai PRD "daftar pengumuman di akun pelanggan begitu mereka login". Nav "Pengumuman" ditambah untuk pelanggan &amp; owner (link berbeda tujuan).
- **Sudah teruji end-to-end lewat curl DAN browser sungguhan**: tamu (tanpa token) cuma lihat pengumuman target "semua pelanggan"; pelanggan yang bukan member &amp; belum pernah booking venue tertentu cuma lihat "semua" juga (dites dengan akun eka); pelanggan yang pernah booking venue 1 tapi bukan member lihat "semua" + "venue" tapi TIDAK "member" — lalu setelah ditandai member (Modul 15), ketiga segmen semua muncul (dites dengan citra, toggle dua arah). Validasi `venue_id` wajib kalau `target_segment=venue` ditolak 422 kalau kosong. Toggle nonaktif langsung menghilangkan pengumuman dari feed publik (dicek ulang API setelah toggle, bukan cuma asumsi UI). Browser: banner beranda tampil untuk tamu, halaman Pengumuman menampilkan ketiga pengumuman untuk pelanggan member, halaman Kelola Pengumuman (owner) berhasil kelola semuanya lewat form &amp; tombol asli — tanpa error console baru.

### Ui-PlayArena (frontend) — Modul 01 (Pengguna & Role) selesai, teruji end-to-end di branch `dev`
- Vite + React 19 + TypeScript + Tailwind CSS v4 + Framer Motion + React Router + lucide-react (persis stack `referensi-play-arena/`, cuma versi production yang disambungkan ke API asli, bukan mock)
- Port dev: **5180**, sudah didaftarkan di `.claude/launch.json` sebagai `playarena-ui`
- `src/lib/api.ts` — fetch wrapper sama seperti Ui-LMS (token di localStorage key `playarena_token`, `api.get/post/put/patch/delete`, class `ApiError`)
- `src/store/AuthContext.tsx` — hydrate sesi dari token tersimpan lewat `GET /me`, `login/register/logout`
- Halaman: `Login`, `Register`, `ForgotPassword`, `ResetPassword`, `Dashboard` (placeholder), `StaffPage` (owner-only — list staff, form tambah staff dengan pemilihan venue, toggle aktif/nonaktif)
- Route guard: `ProtectedRoute` (butuh login), `RoleRoute` (butuh role tertentu, dipakai untuk `/staff`), `GuestRoute` (redirect kalau sudah login)
- **Catatan debugging**: sempat curiga ada bug "Invalid hook call" dari console browser saat testing — setelah diselidiki (repro di komponen minimal, restart server, downgrade Vite 8→6), ternyata itu console log basi yang nyangkut di tab browser lama dari sesi sebelumnya, bukan bug di aplikasi. Tab baru selalu bersih tanpa error. Kalau nemu error serupa nanti, coba dulu di tab baru sebelum curiga ke kode.
- **Sudah teruji end-to-end di browser sungguhan**: login owner → buka Kelola Staff → isi form tambah staff (termasuk klik pilihan venue) → submit → staff baru muncul di tabel → logout → login pakai akun staff yang baru dibuat → coba akses `/staff` → otomatis diarahkan balik ke dashboard (role guard jalan)

## Progress

- [x] PRD selesai & dipublikasikan sebagai artifact (2026-08-27)
- [x] Rebrand GORin → PlayArena, logo asli diterapkan ke PRD (2026-08-27)
- [x] Referensi UI dibuat user (`referensi-play-arena/`, React+Vite+TS mock) (2026-08-27)
- [x] Tabel database `play_arena` dibuat user di PostgreSQL lokal (2026-08-27)
- [x] Setup project backend (Laravel 12 + Sanctum + Spatie Permission, terhubung `play_arena`) (2026-08-27)
- [x] Setup project frontend (Vite + React + TS + Tailwind v4, terhubung ke backend) (2026-08-27)
- [x] **Modul 01 — Pengguna & Role (backend)**: register, login (email/HP), logout, me, reset password, kelola staff + penugasan venue — teruji end-to-end (2026-08-27)
- [x] Repo di-push ke GitHub: github.com/FazriAhmad/play-arena, branch `main` (2026-08-27)
- [x] **Modul 01 — Pengguna & Role (frontend)**: Login, Register, Forgot/Reset Password, Dashboard, Kelola Staff — teruji end-to-end di browser, di-push ke branch `dev` (2026-08-27)
- [x] **Modul 02 — Direktori & Pencarian Lapangan** (backend + frontend): tabel `courts`, direktori publik dengan filter, halaman detail venue + peta Leaflet — teruji end-to-end, di-push ke branch `dev` (2026-08-27)
- [x] **Modul 03 — Kelola Data Lapangan** (backend + frontend): CRUD venue &amp; lapangan penuh untuk Owner (edit, toggle aktif, upload foto, hapus) — teruji end-to-end, di-push ke branch `dev` (2026-08-27)
- [x] **Modul 04 — Kalender & Blokir Slot** (backend + frontend): grid ketersediaan per-jam, blocked_slots per lapangan dikelola owner/staff — teruji end-to-end, di-push ke branch `dev` (2026-08-27)
- [x] **Modul 05 — Booking & Deteksi Bentrok** (backend + frontend, **fitur paling kritis**): EXCLUDE constraint PostgreSQL anti-double-booking, booking pelanggan, auto-expire booking yang tidak di-ACC — teruji end-to-end, di-push ke branch `dev` (2026-08-27)
- [ ] **Modul 06 — Pembayaran Online via Midtrans**: DITUNDA — butuh Server/Client Key Midtrans Sandbox yang cuma bisa didaftarkan user sendiri di sandbox.midtrans.com. Lanjutkan begitu user sudah punya key (atau minta instruksi daftar akun sandbox-nya).
- [x] **Modul 07 — Kelola Booking Masuk**: ACC/reject (reject wajib alasan), konfirmasi pembayaran manual (di luar Midtrans), input booking walk-in — teruji end-to-end (curl + browser), di-push ke branch `dev` (2026-08-27)
- [x] **Modul 08 — Riwayat & Invoice**: riwayat booking pelanggan dikelompokkan (akan datang/selesai/dibatalkan), halaman detail + invoice (cetak/simpan PDF lewat browser, bukan library server), auto-mark booking selesai — teruji end-to-end, di-push ke branch `dev` (2026-08-27)
- [x] **Modul 09 — Cancel, Reschedule & Refund**: kebijakan refund berjenjang per venue, reschedule tunduk penuh Modul 05 (transaction-safe), pembatalan pelanggan (opsional) &amp; Staff/Owner (wajib alasan) — teruji end-to-end, di-push ke branch `dev` (2026-08-27)
- [x] **Modul 10 — Konfirmasi Admin via WhatsApp + Reminder Email**: link wa.me click-to-chat otomatis dibuka saat booking dibuat + follow-up manual setelah 10 menit, reminder email H-1 &amp; H-2jam terjadwal — teruji end-to-end, di-push ke branch `dev` (2026-08-27). **Modul terakhir Fase 1.**
- [x] **Fase 1 (MVP) tuntas** kecuali Modul 06 (Pembayaran Midtrans) yang ditunda
- [x] **Modul 11 — Booking Berulang**: generate sesi mingguan (sampai tanggal atau jumlah sesi), tiap sesi tunduk penuh Modul 05, sesi bentrok ditandai gagal tanpa membatalkan rangkaian — teruji end-to-end, di-push ke branch `dev` (2026-08-28)
- [ ] **Keputusan user (2026-08-28): TETAP di branch `dev`, jangan merge ke `main`** sampai diminta eksplisit — lanjut modul demi modul di Fase 2 dulu.
- [ ] **Modul 12 — Split Payment/Patungan**: DITUNDA (2026-08-28), sama alasan seperti Modul 06 — "tiap peserta bayar sendiri via Midtrans" &amp; "confirmed setelah semua bagian lunas" tidak bisa dibangun tanpa Midtrans. Isi begitu Midtrans sandbox key tersedia, sekalian dengan Modul 06.
- [x] **Modul 13 — Rating & Review**: rating &amp; ulasan per lapangan (cuma booking `completed`, satu review per booking), agregat rating tampil di direktori &amp; halaman venue — teruji end-to-end, di-push ke branch `dev` (2026-08-28)
- [x] **Modul 14 — Voucher & Kode Promo**: kode voucher persen/nominal, syarat minimum, kuota, periode, cakupan per-venue atau semua venue, terintegrasi ke alur booking &amp; pembayaran — teruji end-to-end, di-push ke branch `dev` (2026-08-28)
- [x] **Modul 15 — Kelola Pelanggan/Member**: daftar pelanggan per venue owner, riwayat booking &amp; total belanja, tandai member (prasyarat Modul 21) — teruji end-to-end, di-push ke branch `dev` (2026-08-28)
- [x] **Modul 16 — Promo & Pengumuman (web saja)**: banner beranda + daftar pengumuman akun pelanggan, target segmen (semua/venue tertentu/member), tanpa broadcast WA/Email — teruji end-to-end, di-push ke branch `dev` (2026-08-28)
- [ ] Modul 17–19 — sisa Fase 2 (Multi-Venue &amp; Kelola Staff, Dashboard Analitik, Laporan Pendapatan)
- [ ] Fase 3 — Nice-to-have (Modul 20–21)

## Roadmap Modul (ringkas — detail lengkap ada di PRD)

**Fase 1 (MVP):** Pengguna & Role · Direktori & Pencarian Lapangan · Kelola Data Lapangan · Kalender & Blokir Slot · **Booking & Deteksi Bentrok (kritis)** · Pembayaran Online (DP/Full via Midtrans, setelah ACC) · Kelola Booking Masuk (ACC wajib) · Riwayat & Invoice · Cancel/Reschedule & Refund · **Konfirmasi Admin via WA (manual, click-to-chat) + Reminder Email H-1 & H-2jam**

**Fase 2 (Growth):** Booking Berulang · Split Payment/Patungan · Rating & Review · Voucher & Promo · Kelola Pelanggan · Promo & Pengumuman (web saja) · Multi-Venue & Kelola Staff · Dashboard Analitik · Laporan Pendapatan

**Fase 3 (Nice-to-have):** Sewa Perlengkapan Tambahan · Membership Bulanan
